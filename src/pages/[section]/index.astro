---
export const prerender = false;

const snippets = await fetch(new URL("/registry/helpers.json", Astro.url)).then(
  (res) => res.json(),
);

const firstFunc = snippets[0];

return Astro.redirect(`/${Astro.params.section}/${firstFunc?.name}`, 302);
---
