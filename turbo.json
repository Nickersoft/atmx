{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "preview": {
      "outputs": ["dist/**", ".vercel/**", ".astro/**"]
    },
    "build": {
      "dependsOn": ["^build", "registry#check"],
      "outputs": ["dist/**", ".vercel/**", ".astro/**"]
    },
    "test": {
      "dependsOn": ["^test"]
    },
    "check": {},
    "dev": {
      "dependsOn": ["@atmx-org/registry#check"],
      "persistent": true,
      "cache": false
    }
  }
}