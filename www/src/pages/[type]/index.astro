---
export const prerender = false;

const snippets = await fetch(new URL("/registry/helpers.json", Astro.url)).then(
  (res) => res.json(),
);

return Astro.redirect(`/${Astro.params.type}/${snippets[0]?.name}`, 302);
---
