---
import { cn } from "@/lib/utils";
import { Link } from "@/components/ui/link";

const items = ["helpers", "hooks"];
---

<nav class="border-b z-[9999] relative bg-background">
  <ul class="flex flex-row justify-left items-center gap-4 p-4">
    {
      items.map((type) => {
        const active = Astro.url.pathname.startsWith(`/${type}`);

        return (
          <li>
            <Link
              className={cn(
                "rounded-full font-medium capitalize",
                "hover:text-foreground",
                active && "text-foreground px-2 py-1",
              )}
              href={`/${type}`}
            >
              {type}
            </Link>
          </li>
        );
      })
    }
  </ul>
</nav>
