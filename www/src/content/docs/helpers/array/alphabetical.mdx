---
title: alphabetical
description: Sorts an array of string in alphabetical order
---

import { marked } from "marked";

import Code from "@/components/Code.astro";

import { Typography } from "@/components/ui/typography";
import { Installation } from "@/components/Installation";

import { Tabs, TabItem } from '@astrojs/starlight/components';

import { getSnippets } from "@/lib/get-snippets";


## Usage

```ts
alphabetical(["foo", "bar", "baz"], (item) => item); // ["bar", "baz", "foo"]
```

## Installation

<Tabs>
  <TabItem label="CLI">

    ```
    npx atmx add alphabetical 
    ```
    
  </TabItem>

  <TabItem label="Manually">

    Copy and paste the following method into `@/utils/helpers/alphabetical`:

    ```ts
    /**
     * Sorts an array of string in alphabetical order
     *
     * @param array - The array to sort
     *
     * @example alphabetical(["foo", "bar", "baz"], (item) => item); // ["bar", "baz", "foo"]
     */
    export function alphabetical<T>(
      array: readonly T[],
      getter: (item: T) => string,
      dir: "asc" | "desc" = "asc",
    ) {
      if (!array) return [];
      const asc = (a: T, b: T) => `${getter(a)}`.localeCompare(getter(b));
      const dsc = (a: T, b: T) => `${getter(b)}`.localeCompare(getter(a));
      return array.slice().sort(dir === "desc" ? dsc : asc);
    }
    ```

  </TabItem>
</Tabs>